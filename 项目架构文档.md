# Box 项目架构文档

## 1. 项目概览
- 类型：Android TV 端视频应用（Gradle 多模块）
- 根工程：`TVBox`（`/home/runner/work/Box/Box/settings.gradle`）
- 主要模块：
  - `app`：主应用模块（业务、UI、播放、数据、服务）
  - `quickjs`：QuickJS Android 封装库（JS 执行能力）
- 附加资源：
  - `xwalk/`：Crosswalk 相关 APK 压缩包
  - `app/libs/`：本地三方 jar/aar（如 ffmpeg 解码、xwalk shared lib）

---

## 2. 顶层目录结构
- `/home/runner/work/Box/Box/app`：主应用代码与资源
- `/home/runner/work/Box/Box/quickjs`：QuickJS 库
- `/home/runner/work/Box/Box/xwalk`：Crosswalk 运行时包
- `/home/runner/work/Box/Box/.github/workflows/test.yml`：CI 构建流程（`./gradlew assemblerelease`）

---

## 3. `app` 模块架构

### 3.1 启动与全局初始化
- `com.github.tvbox.osc.base.App`
  - 初始化配置（Hawk）
  - 初始化网络与 EPG
  - 初始化本地数据库（Room）
  - 初始化播放器能力
  - 初始化本地控制服务（AndServer）
  - 初始化 QuickJS 运行环境

### 3.2 分层组织（按包）
- `ui`：界面层  
  - `ui.activity`：页面（Home/Detail/Play/Search/Setting 等）
  - `ui.fragment`：页面片段
  - `ui.dialog`：弹窗交互
  - `ui.adapter`：列表适配器
- `viewmodel`：视图模型层（如 `SourceViewModel`、网盘相关 ViewModel）
- `api`：站点/源配置入口（`ApiConfig`）
- `data`：数据管理（`AppDataManager`、DB helper、搜索逻辑）
- `cache`：Room 实体与 DAO（历史、收藏、缓存、存储盘）
- `player`：播放能力封装  
  - IJK/Exo/第三方播放器接入
  - 控制器与渲染视图
- `subtitle`：字幕解析与渲染引擎（SRT/ASS/TTML 等）
- `server`：本地控制与推送服务（`PlayService`、请求处理）
- `receiver`：系统/外部广播入口
- `util`：工具层（网络、加密、文件、配置、EPG、语言等）
- `bean` / `event`：数据模型与事件总线对象

### 3.3 资源层
- `res/layout`：Activity/Dialog/播放器控制布局
- `res/xml`：网络安全、FileProvider、播放视图配置
- `assets`：`epg_data.json`、`ua.db`、`js/` 脚本资源

---

## 4. `quickjs` 模块职责
- 提供 JS 引擎桥接能力（`QuickJSContext`、`JSObject`、`JSFunction` 等）
- 通过 `jniLibs` 提供 native so（`libquickjs-android-wrapper.so`）
- 被 `app` 依赖，用于脚本化解析/扩展能力

---

## 5. 运行主链路（高层）
1. App 启动（`App.onCreate`）完成全局组件初始化  
2. 进入 `HomeActivity`（Manifest 主入口）  
3. 用户选择数据源/内容后进入详情与播放页  
4. 播放层根据配置选择 IJK/Exo/外部播放器  
5. 字幕、EPG、缓存、历史、收藏由对应子系统协同处理  
6. 本地服务与广播能力支持远程控制/推送交互

---

## 6. 构建与发布
- 构建系统：Gradle + Android Plugin 7.4.2 + Kotlin 1.9.25
- 变体维度：
  - ABI：`armeabi-v7a` / `arm64-v8a`
  - 品牌：`generic` / `hisense`
- CI 默认执行：`./gradlew assemblerelease`
- 输出 APK 命名：`TVBox_<buildType>-<flavors>.apk`

---

## 7. 架构特征总结
- 典型 Android 分包架构（UI / ViewModel / Data / Player / Service）
- 脚本引擎（QuickJS）+ 本地服务（AndServer）增强可扩展性
- 本地 Room + Hawk 形成“结构化数据 + 偏好配置”双存储体系
- 播放器与字幕子系统独立，便于多播放内核并存与演进
